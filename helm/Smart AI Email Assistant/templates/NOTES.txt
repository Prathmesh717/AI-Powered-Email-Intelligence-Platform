Smartai has been installed as release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

------------------------------------------------------------------------
1. Verify the migration job
------------------------------------------------------------------------
The chart runs `alembic upgrade head` as a pre-install / pre-upgrade hook.
Check it succeeded:

  kubectl -n {{ .Release.Namespace }} get job/{{ .Release.Name }}-migrate
  kubectl -n {{ .Release.Namespace }} logs job/{{ .Release.Name }}-migrate

------------------------------------------------------------------------
2. Reach the API
------------------------------------------------------------------------
{{- if .Values.ingress.enabled }}
Via the Ingress (configure DNS + TLS first):

  curl https://{{ .Values.ingress.hosts.api }}/health
{{- else }}
No Ingress is enabled — port-forward to reach the API:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-api 8000:8000
  curl http://localhost:8000/health
{{- end }}

------------------------------------------------------------------------
3. Reach the dashboard
------------------------------------------------------------------------
{{- if .Values.dashboard.enabled }}
{{- if .Values.ingress.enabled }}
  https://{{ .Values.ingress.hosts.dashboard }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ .Release.Name }}-dashboard 8501:8501
  open http://localhost:8501
{{- end }}
{{- else }}
Dashboard is disabled in values.yaml.
{{- end }}

------------------------------------------------------------------------
4. Secret values
------------------------------------------------------------------------
{{- if .Values.secrets.existingSecret }}
Using existing secret: {{ .Values.secrets.existingSecret }}
{{- else }}
A Secret named {{ .Release.Name }}-secrets was created from the values
file. For production, set `secrets.existingSecret` to a sealed-secrets
or external-secrets-operator-managed name instead.
{{- end }}
