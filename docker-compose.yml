version: "3.9"

services:
  # ----------------------------------------------------------------
  # PostgreSQL with pgvector extension
  # ----------------------------------------------------------------
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: Smartai
      POSTGRES_USER: Smartai
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required — see .env.example}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U Smartai -d Smartai"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # ----------------------------------------------------------------
  # Database migrations (run once, then exits)
  # ----------------------------------------------------------------
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    command: alembic upgrade head
    environment:
      POSTGRES_SYNC_URL: postgresql+psycopg://Smartai:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required — see .env.example}@postgres:5432/Smartai
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - migration

  # ----------------------------------------------------------------
  # MCP Tool Server
  # ----------------------------------------------------------------
  mcp_server:
    build:
      context: .
      dockerfile: Dockerfile
      target: mcp
    command: python -m Smartai.mcp.server.main http
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      TAVILY_API_KEY: ${TAVILY_API_KEY:-}
      POSTGRES_URL: postgresql+asyncpg://Smartai:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required — see .env.example}@postgres:5432/Smartai
      MCP_SERVER_HOST: "0.0.0.0"
      MCP_SERVER_PORT: "8001"
    ports:
      - "8001:8001"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ----------------------------------------------------------------
  # FastAPI Backend
  # ----------------------------------------------------------------
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    command: uvicorn Smartai.api.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      OPENAI_MODEL_STRONG: ${OPENAI_MODEL_STRONG:-gpt-4o}
      POSTGRES_URL: postgresql+asyncpg://Smartai:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required — see .env.example}@postgres:5432/Smartai
      POSTGRES_SYNC_URL: postgresql+psycopg://Smartai:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required — see .env.example}@postgres:5432/Smartai
      MCP_SERVER_HOST: mcp_server
      MCP_SERVER_PORT: "8001"
      TAVILY_API_KEY: ${TAVILY_API_KEY:-}
      LANGCHAIN_TRACING_V2: ${LANGCHAIN_TRACING_V2:-false}
      LANGCHAIN_API_KEY: ${LANGCHAIN_API_KEY:-}
      LANGCHAIN_PROJECT: ${LANGCHAIN_PROJECT:-Smartai}
      API_SECRET_KEY: "${API_SECRET_KEY:?API_SECRET_KEY required — generate with: openssl rand -hex 32}"
      DEV_LOGIN_ENABLED: ${DEV_LOGIN_ENABLED:-true}
      DEV_LOGIN_PASSWORD: ${DEV_LOGIN_PASSWORD:?DEV_LOGIN_PASSWORD required when DEV_LOGIN_ENABLED=true}
      DOCS_ENABLED: ${DOCS_ENABLED:-true}
      CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS:-http://localhost:5173,http://localhost:8501}
      TRUSTED_PROXY_COUNT: ${TRUSTED_PROXY_COUNT:-0}
      BUDGET_LIMIT_USD: ${BUDGET_LIMIT_USD:-5.0}
    ports:
      - "8000:8000"
    volumes:
      - ./Smartai:/app/Smartai:ro  # hot-reload in dev
    depends_on:
      postgres:
        condition: service_healthy
      mcp_server:
        condition: service_started
    restart: unless-stopped

  # ----------------------------------------------------------------
  # React + nginx Console
  # Serves the built SPA on :8501 and reverse-proxies /api → api:8000.
  # Auth is now per-user: the SPA hits /api/auth/login and attaches its
  # own JWT. nginx no longer injects a shared admin secret (C-2 fix).
  # ----------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      NGINX_ENVSUBST_TEMPLATE_DIR: /etc/nginx/templates
    ports:
      - "8501:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  pgdata:
    name: Smartai_pgdata
