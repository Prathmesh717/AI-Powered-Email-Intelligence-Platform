# EXAMPLE secret manifest. Copy to 11-secret.yaml and fill in real values
# (or replace entirely with sealed-secrets / external-secrets-operator).
# NEVER commit a real secret file to git.
apiVersion: v1
kind: Secret
metadata:
  name: Smartai-secrets
  namespace: Smartai
  labels:
    app.kubernetes.io/name: Smartai
type: Opaque
stringData:
  # --- Required ---
  OPENAI_API_KEY: "sk-replace-me"
  API_SECRET_KEY: "generate-with-openssl-rand-hex-32"
  POSTGRES_PASSWORD: "generate-a-strong-password"

  # --- Optional integrations (leave blank to disable) ---
  TAVILY_API_KEY: ""
  LANGCHAIN_API_KEY: ""
  SLACK_BOT_TOKEN: ""
  GITHUB_TOKEN: ""
  JIRA_API_TOKEN: ""
  HUBSPOT_ACCESS_TOKEN: ""
  SALESFORCE_ACCESS_TOKEN: ""
  SERVICENOW_PASSWORD: ""
  SAP_PASSWORD: ""
  QUICKBOOKS_ACCESS_TOKEN: ""
  MSGRAPH_ACCESS_TOKEN: ""
  ANTHROPIC_API_KEY: ""
