apiVersion: v1
kind: Service
metadata:
  name: Smartai-postgres
  namespace: Smartai
  labels:
    app.kubernetes.io/name: Smartai
    app.kubernetes.io/component: postgres
spec:
  # Headless service for StatefulSet stable DNS.
  clusterIP: None
  selector:
    app.kubernetes.io/name: Smartai
    app.kubernetes.io/component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
